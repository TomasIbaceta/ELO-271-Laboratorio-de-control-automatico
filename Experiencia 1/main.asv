%%experiencia 1 - actividad 1
%y_s = y sampleado cada 0.5s
%---------------------
%datos para la entrada de simulink
a1 = 1;
a2 = 2;

%datos de salida de simulink
y_k = out.y_k;
u1_k = out.step;
u2_k = out.sin;
%--
k = [0 : 1 : length(y_s)-1]; %base de tiempo discreta en pasos
t = linspace(0,10,length(y_s)); %base de tiempo discreta en [s]
stem(t,y_k);

A = [transpose(u1_k) 
%----------------
